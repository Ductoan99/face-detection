import React from 'react'

const Navigation = ()=>{
    return(
        <nav style={{display:'flex',justifyContent:'flex-end', padding:25}}>
            <button  type="button" className="btn btn-success">Đăng xuất</button>
        </nav>
    )
}

export default Navigation;